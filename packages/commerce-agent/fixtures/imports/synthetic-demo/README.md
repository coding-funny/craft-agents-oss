# Synthetic commerce import

This directory contains generated demonstration records. They are not merchant exports and must not be described as real production data. `manifest.json` uses `SYNTHETIC_FIXTURE` so that provenance remains machine-readable.
