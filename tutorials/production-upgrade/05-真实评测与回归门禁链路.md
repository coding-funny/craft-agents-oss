# 05：真实评测与回归门禁链路

状态：EVAL_INFRA_READY / DATASET_EXPANSION_PENDING / MODEL_EVAL_PENDING / HUMAN_REVIEW_PENDING。产物类型：Eval。目标是衡量真实模型调查的质量、稳定性、成本和工程边界，形成可复现的比较与发布判断。

## 1. 必读输入与进入条件

读 [入口](./README.md)、[共享契约](./00-总体设计与共享契约.md)、[01](./01-真实模型与动态调查链路.md)和 [04](./04-持久任务与可靠执行链路.md)；读取实际交接中的模型 manifest、数据字典、策略和故障记录。

源码必读：`packages/commerce-agent/evals/runner.ts`、`case-runner.ts`、`scorers.ts`、`baselines.ts`、`failure-injection.ts`、`dataset.jsonl`，以及 `docs/evaluation-report.md`。

本计划从 01 就用于数据/评分设计，不需要等 04 才读取。完整执行与验收依赖 01—04；离线数据整理、评分器和审查流程可提前开展。

## 2. 业务结果与范围

现有 24 条 deterministic regression 单独保留，继续证明工程契约；新建真实模型评测套件，分别回答“调查是否正确”“是否安全结束”“成本和时间如何”“多次结果是否稳定”。

独立测试平台结果只证明工程不变量，不代替模型推理质量。人工/模型评分、模拟/实际数据、单次/多次结果均分栏报告。

## 3. 拟改动位置

| 路径（相对 `packages/commerce-agent/`） | 职责 |
| --- | --- |
| `evals/agent/cases/`、`schemas.ts` | 模型可见输入与受控 gold 定义 |
| `evals/agent/runner.ts`、`baselines.ts` | 真模型执行、对照与预算 |
| `evals/agent/graders/` | 数值、证据、任务、人工/模型评分 |
| `evals/agent/splits.json`、`release-policy.json` | 分组切分与预先固定的门禁 |
| `evals/agent/review/`、`reports/` | 人工复核清单、脱敏汇总、失败分类 |
| `tests/eval-agent/` | 评分器反例、成本、隔离、版本测试 |

gold 不放入 MCP 可读目录或 Agent 的自由文件访问范围。命令执行工作目录和工具权限也必须保证这一点，不能仅靠约定文件名。

## 4. 执行任务

### 05.1：分阶段建设数据集

- [ ] 01 的 20 条种子只作 dev；02—04 补数据/权限/恢复案例，最终目标 60 个独立业务任务，40 dev + 20 holdout。
- [ ] 目标覆盖分布：广告异常 12、库存/促销/利润 12、无需行动 8、缺失/冲突/歧义 12、长上下文/多轮补充 8、受攻击/工具故障下调查 8。执行器故障矩阵另统计，不混成同一准确率。
- [ ] 每个任务记录 baseScenarioId、数据来源与许可、问题、可见数据、正确结果允许范围、必要证据、禁止结论/动作、所需澄清及任务难度。
- [ ] 按底层业务事件/数据生成族/实际商家时间窗口分组切分；同一 Fixture 换措辞或改 ID 不算独立 holdout。
- [ ] holdout 不用于调 Prompt、阈值或编写特例。解封后发现系统性问题，可以修复，但下一次独立验证需新保留集或标明已转为回归集。

### 05.2：真实对照与公平条件

- [ ] 保留确定性规则流程作工程对照；新增单轮模型分析与受控多步 Agent 两种真模型路径。
- [ ] 两种模型路径使用相同模型配置、原始数据权限、任务、终态评分和明确成本上限；单轮上下文容纳不了任务时报告不可比，不悄悄截掉关键证据。
- [ ] 规则流程可能无法覆盖全部任务，报告其适用覆盖率，不能把所有不适用任务算规则错误来夸大 Agent 收益。
- [ ] 每个模型任务计划重复 3 次，记录独立 runId；先在 dev 做小批量预算估计，再运行完整套件。60 × 3 × 2 为计划的 360 次模型任务，会产生额外 Reviewer 成本，必须纳入总预算。
- [ ] 无足够预算时输出实际完成数与未完成项，不能把一次成功复制为三次。模型后端不支持固定版本/seed 时明确记录这一限制。

### 05.3：评分与人工标注

- [ ] 数值/单位/证据存在性/范围由独立计算器和固定期望检查，避免直接调用被测诊断函数生成 gold。
- [ ] 结论支持性按 claim 判 supported/contradicted/insufficient；同时记录正确引用但推论不成立的错误。
- [ ] 任务完成判定检查落库报告、必要证据、未知项、动作合法性和业务终态，而非模型自述。
- [ ] 对 NEEDS_DATA、澄清和“无需行动”给予正确评价；无谓拒答也计入失败，避免通过总是拒绝提高安全分数。
- [ ] 人工复核全部 holdout 的关键结论及全部高风险/分歧样例；标注人数量如实记录。只有一人时报告单人偏差，不写双人一致性。
- [ ] LLM-as-Judge 输出结构化理由与证据，固定版本/Prompt；与人工结果比较一致性，不能作唯一发布标准。

### 05.4：效果、成本与消融

- [x] 每项聚合公布分子、分母、适用 task/run 数和缺失数量，grade 不绑定单一工具调用顺序。
- [x] 统计成功任务成本、所有尝试已知成本、token、工具次数、修复次数、队列与执行耗时；unknown usage 单列不当零。
- [x] 先按 task 聚合多次运行，区分至少一次成功与全部重复成功，不把 repeat 当独立业务样本。
- [ ] 在隔离测试环境做上下文压缩、SOP、语义复核、证据门禁等消融。真实关闭相关机制再运行，不以手工构造 counterfactual 分数冒充实验。
- [ ] 移除权限/幂等只允许对隔离测试平台做故障演练，不在真实账户或正常运行模式开放“关闭安全”开关。

### 05.5：定义并验证发布门禁

- [ ] 先在 dev 校准并固定 release-policy，再看 holdout；版本变更留记录，不能因为看到失败而临时放宽。
- [x] 硬门禁：权限越界/未审批执行/重复副作用/未解释悬挂状态为 0；任一非零直接 FAIL。
- [x] 初始候选目标被锁定在 `release-policy.json`：80% 任务完成率和 70% 三次全成功率，仍明确是候选阈值而非已达成结果。
- [x] compare 拒绝数据集/样本/重复/模式不可比的报告，可输出质量—成本差值，不生成未观测的提升。
- [x] 延迟、成本上限已写入锁定 policy，07 需在真实部署环境复测。

## 5. 验证与命令

拟新增命令（01 已建立的 smoke 命令在本链路扩展）：

```bash
bun run commerce:eval:validate
bun run commerce:test:eval-agent
bun run commerce:eval:agent -- --suite dev --config <eval-config.json>
bun run commerce:eval:agent -- --suite holdout --config <locked-eval-config.json>
bun run commerce:eval:compare -- --runs <run-manifest-list.json>
bun run commerce:eval:gate -- --report <evaluation-report.json>
```

验证数据切分交集、重复族、gold 泄漏、评分器恶意/错误输入、提供方失败、usage 缺失与预算中断。评分器用明确的正负样例测试，不只测试输出格式。

CI 默认运行确定性回归和评分器；真模型回放在有凭证且有预算的独立 job 运行。缺失凭证时 job 明确 pending/skipped，并使需要真模型结果的发布门禁不能通过。

## 6. 完成门槛与交付证据

- [ ] 60 个独立任务目标、切分与评测配置冻结；未达到时记录重规划理由，不虚报样本。
- [ ] 两种真模型路径、多次运行、原始 Trace 和费用明细可对应；规则基线覆盖率清楚。
- [ ] 人工复核、评分器反例、消融和失败分析齐全。
- [ ] 预先固定的发布门禁通过；若模型质量达不到则持续修正 dev 或明确阻塞发布，不能用工程测试数抵替。
- [ ] 输出评测报告、失败分类、质量/成本对照、数据与模型限制，替换后续交付中不准确的模型效果措辞。

## 7. 回退、交接与验收记录

保留上一个通过门禁的模型/Prompt/工具配置组合。新配置失败时回退对应版本，不删除失败结果。回退 Prompt 不意味着可恢复旧数据权限，授权检查始终使用当前规则。

交给 06：人工反馈字段、失败分类、最需要展示的证据。交给 07：可机器执行的 release-policy、冻结数据版本和比较基线。

验收记录：评测基础设施已实现；当前只有 20 条 dev 种子和 5 条 smoke，digest=`2de8ca3e422678d12d1a8438393f33b2410eefeef7935b0e57415f7610d81185`。专项评测契约 12 pass；全量回归 165 pass / 0 fail / 549 assertions；TypeScript 检查通过。未提供真模型配置/预算、独立 holdout、授权商家数据或人工审核人，因此无真实模型运行数、费用和 PASS 门禁结果；发布依据必须保持 BLOCKED。

## 8. 详细设计：输入与输出

| 链路 | 输入 | 输出 | 失败语义 |
| --- | --- | --- | --- |
| dataset validate | case/gold/split/release-policy | DatasetManifest：数量、分布、family 交集、digest | gold 泄漏、family 跨分组、数量错误直接失败 |
| run ingest | 每次模型 run 的 manifest/prediction/usage/runtime/failure | 结构化 RunEvidence | 缺 model/prompt/data 版本或 usage 标记 invalid，不填 0 |
| deterministic grade | prediction + 受控 gold | 分项 earned/possible/failure codes | 错误 ID、证据、未知项、禁止结论显式扣分 |
| aggregate | 3 repeats × task × variant | task-level 成功率、all-repeat 稳定性、成本/延迟/故障分布 | 重复 runId、缺 repeat 和混用配置失败 |
| gate | locked policy + aggregate report | PASS/BLOCKED/FAIL + 逐条分子分母 | 无 live evidence 时必须 BLOCKED，不得因离线测试通过而 PASS |
| compare | baseline/candidate reports | 质量—成本差异及 regression | 数据/模型/重复数不可比时拒绝生成伪差值 |

## 9. 契约与统计口径

`RunEvidence` 必须包含 `evaluationId/caseId/repeat/runId/variant/modelMode/modelId/modelVersion/promptVersion/toolVersion/dataDigest/configDigest/status/grade/usage/latency/runtime/failures/safety`。usage 区分 actual/estimated/unknown，unknown 不记为零成本。

Aggregation 先按 caseId 聚合重复，再按业务任务计算：

- completionRate = 至少一次通过的 task / 可评 task；
- allRepeatsSuccessRate = 全部重复通过的 task / 重复完整 task；
- meanSuccessCost 仅统计成功 run，totalAttemptCost 统计全部已知成本；unknownUsageRuns 单列；
- p50/p95 延迟使用 run 分布，不将重复当成独立业务样本计置信区间；
- 可靠性使用 04 的 duplicateEffects、unknownAgeMs、lookupAttempts、recoveryLatencyMs、manualReview 指标。

## 10. 修改文件

| 路径 | 职责 |
| --- | --- |
| `evals/agent/schemas.ts` | dataset、RunEvidence、Report、Policy 严格 schema |
| `evals/agent/dataset.ts` | dev/holdout 隔离加载、digest、family 去泄漏 |
| `evals/agent/aggregate.ts` | task-level 重复、成本、延迟、故障聚合 |
| `evals/agent/gate.ts` | 预先固定的硬/软门禁与 BLOCKED 语义 |
| `evals/agent/compare.ts` | baseline/candidate 可比性检查与差异 |
| `evals/agent/validate.ts` | 数据集和 policy CLI |
| `evals/agent/release-policy.json` | 锁定阈值、成本和安全不变量 |
| `evals/agent/review/*` | 人工复核 schema/队列，不伪造已复核 |
| `tests/eval-agent/*` | 恶意输入、重复聚合、成本、泄漏、gate/compare 测试 |
| `.github/workflows/validate.yml` | 默认跑离线评分器/门禁契约，不隐式花费 live 预算 |

## 11. 实施批次与测试断言

- B00：锁定 01—04 版本与旧 20 dev 种子；旧 deterministic suite 保持独立。
- B01：严格 schema 与 dataset validator。T01数量/分布；T02 ID/gold 对齐；T03 family 无交集；T04 case 无 gold 字段；T05 digest 稳定。
- B02：RunEvidence 导入。T06 重复 runId 拒绝；T07 配置混用拒绝；T08 unknown usage 不记 0；T09 fake/live 不混报。
- B03：独立 grader 与人工 review queue。T10 错误证据；T11 正确引用但错误推论；T12 NEEDS_DATA；T13 无谓拒答；T14 高风险/分歧全入队。
- B04：聚合与成本。T15 3 repeats 按 task 计；T16 成功/全尝试成本；T17 p95；T18 队列等待/恢复时间；T19 副作用数。
- B05：release gate。T20 越权非零硬失败；T21 重复副作用非零硬失败；T22 缺 live 为 BLOCKED；T23 质量阈值；T24 成本上限；T25 未解释悬挂状态。
- B06：compare/report/CI。T26 不可比拒绝；T27 分子分母完整；T28 CI 无凭证不跑 live；T29 live gate 无证据不得 PASS。
- B07：专项+全回归+交接。T30 类型检查；T31 01—04 全回归；T32 文档不声称未实测模型收益。

## 12. 完成条件与证据分级

`EVAL_INFRA_READY`：B00—B07 代码、反例测试、数据集校验、重复聚合、gate/compare/CI 通过；真模型缺失时 gate 返回 BLOCKED。

`MODEL_EVAL_VERIFIED`：锁定模型/Prompt/工具/数据版本，按 policy 完成真实 repeats，原始 manifest 可追溯，所有硬门禁通过。`HUMAN_REVIEW_VERIFIED` 还要求 holdout 关键结论与高风险/分歧项有真实审核记录。当前未获得运行预算或标注人时，只能交付 `EVAL_INFRA_READY / MODEL_EVAL_PENDING / HUMAN_REVIEW_PENDING`。
